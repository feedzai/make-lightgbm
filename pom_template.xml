<project>
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.feedzai.openml.lightgbmlib</groupId>
  <artifactId>lightgbmlib</artifactId>
  <version>${VERSION}</version>
  <packaging>jar</packaging>
  <url>${MAKE_LIGHTGBM_URL}</url>
  <description>
    LightGBM build for Java generated with make-lightgbm.

    Build created with command: `bash make.sh ${LIGHTGBM_VERSION} ${PACKAGE_VERSION}`

    ---
    
    Build info:
    
    LIGHTGBM_VERSION=${LIGHTGBM_VERSION}
    LIGHTGBM_COMMIT=${COMMIT}
    PACKAGE_TIMESTAMP=${PACKAGE_TIMESTAMP}
  </description>
</project>
